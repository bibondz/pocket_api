const express = require('express');
const router = express.Router();

// Placeholder routes for tank permissions
router.get('/', (req, res) => {
    res.json({ message: 'Tank permission routes placeholder' });
});

module.exports = router; 